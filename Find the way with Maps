# define a function to triple a number
def triple(x):
  return x * 3

# create the list of integers
numbers = [1, 2, 3, 4, 5, 6, 7]

# triple the numbers in the list using the map function
tripled = list(map(triple, numbers))

# print the triple of each number
print(tripled)

The output of this program will be:
[3, 6, 9, 12, 15, 18, 21]
